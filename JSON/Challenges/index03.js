let myTeam = {
    "Team": "Argentina"
}

/*
Now add two more key-value pairs to your `myTeam` object. One for `Sport` with a value of `Football`, the other for `Year` with a value of `1986` (this should be a number, not a string).

Remember that key-value pairs in objects are separated by commas.
*/
